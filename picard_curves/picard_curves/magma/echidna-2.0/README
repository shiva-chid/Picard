This version of the Echidna package contains everything needed for the calculations.

Furthermore, it fixes a few tiny errors in Kohel's code that caused the old version to occasionally crash or return wrong results in non-generic cases.